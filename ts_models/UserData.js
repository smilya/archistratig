export {};
/* export default class UserData {
  firstName: string
  lastName: string
  age: number | ''
  isMale: boolean

  constructor(model?: any) {
    this.firstName = model.firstName || ''
    this.lastName = model.lastName || ''
    this.age = model.age || ''
    this.isMale = model.isMale || false
  }
} */ 
